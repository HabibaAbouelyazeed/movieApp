import { RateToPercentPipe } from './rate-to-percent.pipe';

describe('RateToPercentPipe', () => {
  it('create an instance', () => {
    const pipe = new RateToPercentPipe();
    expect(pipe).toBeTruthy();
  });
});
